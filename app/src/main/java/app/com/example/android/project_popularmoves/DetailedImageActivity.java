package app.com.example.android.project_popularmoves;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class DetailedImageActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_detailed_image);
//        DetailedFragment detailedFragment = new DetailedFragment();
//        getSupportFragmentManager().beginTransaction().add(R.id.fldetail,detailedFragment,"").commit();
    }
}
